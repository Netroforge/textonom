import {
  computeTextDiff,
  formatDiffAsText,
  computeWordDiff,
  type DiffLine,
  type WordSegment
} from './text-diff'

export { computeTextDiff, formatDiffAsText, computeWordDiff, type DiffLine, type WordSegment }
